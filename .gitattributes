# OpenIM public distribution line-ending policy.
# Shell and JavaScript launcher files use LF on every checkout. Model Markdown
# and host-side files keep the operator's native line-ending behaviour.
*.sh  text eol=lf
*.mjs text eol=lf

# Generated SVG/HTML retains renderer whitespace verbatim.
exports/diagrams/** -whitespace
