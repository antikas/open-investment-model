{
  "components": {
    "parameters": {
      "delay": {
        "description": "Specify the delay to execute the operation, for more info check the [delay documentation](https://docs.restate.dev/invoke/http#sending-a-delayed-message-over-http)",
        "example": "10s",
        "in": "query",
        "name": "delay",
        "required": false,
        "schema": {
          "type": "string"
        }
      },
      "idempotencyKeyHeader": {
        "description": "Idempotency key to execute the request, for more details checkout the [idempotency key documentation](https://docs.restate.dev/invoke/http#invoke-a-handler-idempotently).",
        "in": "header",
        "name": "idempotency-key",
        "required": false,
        "schema": {
          "type": "string"
        }
      },
      "idempotencyKeyPath": {
        "description": "Idempotency key used to execute the original request, for more details checkout the [idempotency key documentation](https://docs.restate.dev/invoke/http#invoke-a-handler-idempotently).",
        "in": "path",
        "name": "idempotencyKey",
        "required": true,
        "schema": {
          "type": "string"
        }
      },
      "key": {
        "in": "path",
        "name": "key",
        "required": true,
        "schema": {
          "type": "string"
        }
      }
    },
    "responses": {
      "GenericError": {
        "content": {
          "application/json": {
            "example": {
              "code": 500,
              "description": "Very bad error happened",
              "message": "Internal server error"
            },
            "schema": {
              "$ref": "#/components/schemas/RestateError"
            }
          }
        },
        "description": "Error response"
      },
      "InvocationNotFoundError": {
        "content": {
          "application/json": {
            "example": {
              "code": 404,
              "message": "Not found"
            },
            "schema": {
              "$ref": "#/components/schemas/RestateError"
            }
          }
        },
        "description": "Invocation not found"
      },
      "InvocationNotReadyError": {
        "content": {
          "application/json": {
            "example": {
              "code": 470,
              "message": "Not ready"
            },
            "schema": {
              "$ref": "#/components/schemas/RestateError"
            }
          }
        },
        "description": "Invocation still running, output is not ready yet"
      },
      "Send": {
        "content": {
          "application/json": {
            "example": {
              "invocationId": "inv_1gdJBtdVEcM942bjcDmb1c1khoaJe11Hbz",
              "status": "Accepted"
            },
            "schema": {
              "additionalProperties": false,
              "properties": {
                "executionTime": {
                  "description": "Time when the invocation will be executed, in case 'delay' is used",
                  "format": "date-time",
                  "type": "string"
                },
                "invocationId": {
                  "type": "string"
                },
                "status": {
                  "enum": [
                    "Accepted",
                    "PreviouslyAccepted"
                  ],
                  "type": "string"
                }
              },
              "required": [
                "invocationId",
                "status"
              ],
              "title": "RestateSendResponse",
              "type": "object"
            }
          }
        },
        "description": "Send response"
      }
    },
    "schemas": {
      "RestateError": {
        "additionalProperties": false,
        "description": "Generic error used by Restate to propagate Terminal errors/exceptions back to callers",
        "properties": {
          "code": {
            "title": "error code",
            "type": "number"
          },
          "message": {
            "title": "Error message",
            "type": "string"
          },
          "metadata": {
            "additionalProperties": {
              "type": "string"
            },
            "title": "Additional metadata map",
            "type": "object"
          },
          "stacktrace": {
            "title": "Stacktrace of the error",
            "type": "string"
          }
        },
        "required": [
          "message"
        ],
        "title": "RestateError",
        "type": "object"
      },
      "execute_soRequest": {
        "additionalProperties": false,
        "description": "The ``execute_so`` request envelope \u2014 a named SO plus its argument dict.\n\nA **Pydantic model** (not a bare ``TypedDict``) so the Restate auto-generated OpenAPI/MCP\nsurface derives a *real, typed* request schema from it \u2014 the envelope is typed at the boundary\n(goal (c)): a malformed body that is not this shape is rejected by the SDK's typed deserialiser\nas a ``TerminalError`` (no retry) before the handler runs. ``soId`` is the Service-Operation\nidentifier (e.g. ``\"SO-09-01\"``); ``args`` is the tool's input as a plain object, mapped onto\nthe tool's own ``extra=\"forbid\"`` Pydantic input inside the journaled step.\n\nThe envelope itself is permissive on ``args`` (an arbitrary object) on purpose \u2014 the *per-tool*\nvalidation (which argument keys are allowed) belongs to each tool's input model, surfaced via\n``list_capabilities``; the envelope only types the *transport shape*.",
        "properties": {
          "args": {
            "additionalProperties": true,
            "description": "The tool's arguments as a JSON object, mapped onto the tool's typed input.",
            "title": "Args",
            "type": "object"
          },
          "soId": {
            "description": "The Service-Operation identifier to dispatch, e.g. 'SO-09-01'.",
            "title": "Soid",
            "type": "string"
          }
        },
        "required": [
          "soId"
        ],
        "title": "ExecuteSoInput",
        "type": "object"
      },
      "execute_soResponse": true,
      "list_capabilitiesResponse": true
    }
  },
  "info": {
    "title": "bd09",
    "version": "1"
  },
  "openapi": "3.1.0",
  "paths": {
    "/bd09/execute_so": {
      "post": {
        "description": "Dispatch a named BD-09 Service Operation to its tool as a journaled durable step.\n\nModel-free dispatch \u2014 it routes the *named* ``soId`` to its registered tool; it does not decide\nwhich SO to call (that is the orchestrator's one planning loop, built separately). An unknown\n``soId`` is a ``TerminalError`` (Restate does not retry it). The ``args`` are mapped onto the\ntool's Pydantic input inside the journaled step; a bad/extra/missing argument or a deterministic\ncompute failure (including the non-conventional-cash-flow fail-loud) is likewise terminal \u2014\nnever a retry storm.\n\nThe request envelope is the ``ExecuteSoInput`` Pydantic model, so the Restate auto-generated\nOpenAPI/MCP surface types it and the SDK's typed deserialiser rejects a non-object body as a\n``TerminalError`` (no retry) before this handler runs; ``_coerce_envelope`` carries the same\nguard for the programmatic path. A malformed non-dict body is therefore terminal at the ingress\n(the carry-forward), not a bounded retry.\n\nProvenance is replay-safe: it is derived only from the request and the tool's deterministic\noutput (no wall-clock is read inside the journaled step).",
        "operationId": "execute_so",
        "parameters": [
          {
            "$ref": "#/components/parameters/idempotencyKeyHeader"
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/execute_soRequest"
              }
            }
          },
          "required": false
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/execute_soResponse"
                }
              }
            },
            "description": ""
          },
          "default": {
            "$ref": "#/components/responses/GenericError"
          }
        },
        "summary": "execute_so"
      }
    },
    "/bd09/execute_so/send": {
      "post": {
        "description": "Dispatch a named BD-09 Service Operation to its tool as a journaled durable step.\n\nModel-free dispatch \u2014 it routes the *named* ``soId`` to its registered tool; it does not decide\nwhich SO to call (that is the orchestrator's one planning loop, built separately). An unknown\n``soId`` is a ``TerminalError`` (Restate does not retry it). The ``args`` are mapped onto the\ntool's Pydantic input inside the journaled step; a bad/extra/missing argument or a deterministic\ncompute failure (including the non-conventional-cash-flow fail-loud) is likewise terminal \u2014\nnever a retry storm.\n\nThe request envelope is the ``ExecuteSoInput`` Pydantic model, so the Restate auto-generated\nOpenAPI/MCP surface types it and the SDK's typed deserialiser rejects a non-object body as a\n``TerminalError`` (no retry) before this handler runs; ``_coerce_envelope`` carries the same\nguard for the programmatic path. A malformed non-dict body is therefore terminal at the ingress\n(the carry-forward), not a bounded retry.\n\nProvenance is replay-safe: it is derived only from the request and the tool's deterministic\noutput (no wall-clock is read inside the journaled step).",
        "operationId": "execute_soSend",
        "parameters": [
          {
            "$ref": "#/components/parameters/idempotencyKeyHeader"
          },
          {
            "$ref": "#/components/parameters/delay"
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/execute_soRequest"
              }
            }
          },
          "required": false
        },
        "responses": {
          "200": {
            "$ref": "#/components/responses/Send"
          },
          "202": {
            "$ref": "#/components/responses/Send"
          },
          "default": {
            "$ref": "#/components/responses/GenericError"
          }
        },
        "summary": "execute_so",
        "tags": [
          "Send"
        ]
      }
    },
    "/bd09/list_capabilities": {
      "post": {
        "description": "Return the registered BD-09 tool catalogue \u2014 each so_id with its real I/O JSON schema.\n\nThe schemas are the live Pydantic ``model_json_schema()`` of each tool's input and output, the\nsingle source the MCP and OpenAPI surfaces (built separately) generate from. Read-only; no\njournaled step needed (it derives from the static registry).\n\nInvocation note: this handler takes no input argument, so over the Restate HTTP ingress it is\ncalled with an empty request body (``POST /bd09/list_capabilities`` with no body) \u2014 a JSON body\nis rejected. ``execute_so``, which takes an input, is called with the JSON request envelope.",
        "operationId": "list_capabilities",
        "parameters": [
          {
            "$ref": "#/components/parameters/idempotencyKeyHeader"
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/list_capabilitiesResponse"
                }
              }
            },
            "description": ""
          },
          "default": {
            "$ref": "#/components/responses/GenericError"
          }
        },
        "summary": "list_capabilities"
      }
    },
    "/bd09/list_capabilities/send": {
      "post": {
        "description": "Return the registered BD-09 tool catalogue \u2014 each so_id with its real I/O JSON schema.\n\nThe schemas are the live Pydantic ``model_json_schema()`` of each tool's input and output, the\nsingle source the MCP and OpenAPI surfaces (built separately) generate from. Read-only; no\njournaled step needed (it derives from the static registry).\n\nInvocation note: this handler takes no input argument, so over the Restate HTTP ingress it is\ncalled with an empty request body (``POST /bd09/list_capabilities`` with no body) \u2014 a JSON body\nis rejected. ``execute_so``, which takes an input, is called with the JSON request envelope.",
        "operationId": "list_capabilitiesSend",
        "parameters": [
          {
            "$ref": "#/components/parameters/idempotencyKeyHeader"
          },
          {
            "$ref": "#/components/parameters/delay"
          }
        ],
        "responses": {
          "200": {
            "$ref": "#/components/responses/Send"
          },
          "202": {
            "$ref": "#/components/responses/Send"
          },
          "default": {
            "$ref": "#/components/responses/GenericError"
          }
        },
        "summary": "list_capabilities",
        "tags": [
          "Send"
        ]
      }
    },
    "/restate/invocation/bd09/execute_so/{idempotencyKey}/attach": {
      "get": {
        "description": "Dispatch a named BD-09 Service Operation to its tool as a journaled durable step.\n\nModel-free dispatch \u2014 it routes the *named* ``soId`` to its registered tool; it does not decide\nwhich SO to call (that is the orchestrator's one planning loop, built separately). An unknown\n``soId`` is a ``TerminalError`` (Restate does not retry it). The ``args`` are mapped onto the\ntool's Pydantic input inside the journaled step; a bad/extra/missing argument or a deterministic\ncompute failure (including the non-conventional-cash-flow fail-loud) is likewise terminal \u2014\nnever a retry storm.\n\nThe request envelope is the ``ExecuteSoInput`` Pydantic model, so the Restate auto-generated\nOpenAPI/MCP surface types it and the SDK's typed deserialiser rejects a non-object body as a\n``TerminalError`` (no retry) before this handler runs; ``_coerce_envelope`` carries the same\nguard for the programmatic path. A malformed non-dict body is therefore terminal at the ingress\n(the carry-forward), not a bounded retry.\n\nProvenance is replay-safe: it is derived only from the request and the tool's deterministic\noutput (no wall-clock is read inside the journaled step).",
        "operationId": "execute_soAttach",
        "parameters": [
          {
            "$ref": "#/components/parameters/idempotencyKeyPath"
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/execute_soResponse"
                }
              }
            },
            "description": ""
          },
          "404": {
            "$ref": "#/components/responses/InvocationNotFoundError"
          },
          "default": {
            "$ref": "#/components/responses/GenericError"
          }
        },
        "summary": "execute_so",
        "tags": [
          "Attach"
        ]
      }
    },
    "/restate/invocation/bd09/execute_so/{idempotencyKey}/output": {
      "get": {
        "description": "Dispatch a named BD-09 Service Operation to its tool as a journaled durable step.\n\nModel-free dispatch \u2014 it routes the *named* ``soId`` to its registered tool; it does not decide\nwhich SO to call (that is the orchestrator's one planning loop, built separately). An unknown\n``soId`` is a ``TerminalError`` (Restate does not retry it). The ``args`` are mapped onto the\ntool's Pydantic input inside the journaled step; a bad/extra/missing argument or a deterministic\ncompute failure (including the non-conventional-cash-flow fail-loud) is likewise terminal \u2014\nnever a retry storm.\n\nThe request envelope is the ``ExecuteSoInput`` Pydantic model, so the Restate auto-generated\nOpenAPI/MCP surface types it and the SDK's typed deserialiser rejects a non-object body as a\n``TerminalError`` (no retry) before this handler runs; ``_coerce_envelope`` carries the same\nguard for the programmatic path. A malformed non-dict body is therefore terminal at the ingress\n(the carry-forward), not a bounded retry.\n\nProvenance is replay-safe: it is derived only from the request and the tool's deterministic\noutput (no wall-clock is read inside the journaled step).",
        "operationId": "execute_soOutput",
        "parameters": [
          {
            "$ref": "#/components/parameters/idempotencyKeyPath"
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/execute_soResponse"
                }
              }
            },
            "description": ""
          },
          "404": {
            "$ref": "#/components/responses/InvocationNotFoundError"
          },
          "470": {
            "$ref": "#/components/responses/InvocationNotReadyError"
          },
          "default": {
            "$ref": "#/components/responses/GenericError"
          }
        },
        "summary": "execute_so",
        "tags": [
          "Get Output"
        ]
      }
    },
    "/restate/invocation/bd09/list_capabilities/{idempotencyKey}/attach": {
      "get": {
        "description": "Return the registered BD-09 tool catalogue \u2014 each so_id with its real I/O JSON schema.\n\nThe schemas are the live Pydantic ``model_json_schema()`` of each tool's input and output, the\nsingle source the MCP and OpenAPI surfaces (built separately) generate from. Read-only; no\njournaled step needed (it derives from the static registry).\n\nInvocation note: this handler takes no input argument, so over the Restate HTTP ingress it is\ncalled with an empty request body (``POST /bd09/list_capabilities`` with no body) \u2014 a JSON body\nis rejected. ``execute_so``, which takes an input, is called with the JSON request envelope.",
        "operationId": "list_capabilitiesAttach",
        "parameters": [
          {
            "$ref": "#/components/parameters/idempotencyKeyPath"
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/list_capabilitiesResponse"
                }
              }
            },
            "description": ""
          },
          "404": {
            "$ref": "#/components/responses/InvocationNotFoundError"
          },
          "default": {
            "$ref": "#/components/responses/GenericError"
          }
        },
        "summary": "list_capabilities",
        "tags": [
          "Attach"
        ]
      }
    },
    "/restate/invocation/bd09/list_capabilities/{idempotencyKey}/output": {
      "get": {
        "description": "Return the registered BD-09 tool catalogue \u2014 each so_id with its real I/O JSON schema.\n\nThe schemas are the live Pydantic ``model_json_schema()`` of each tool's input and output, the\nsingle source the MCP and OpenAPI surfaces (built separately) generate from. Read-only; no\njournaled step needed (it derives from the static registry).\n\nInvocation note: this handler takes no input argument, so over the Restate HTTP ingress it is\ncalled with an empty request body (``POST /bd09/list_capabilities`` with no body) \u2014 a JSON body\nis rejected. ``execute_so``, which takes an input, is called with the JSON request envelope.",
        "operationId": "list_capabilitiesOutput",
        "parameters": [
          {
            "$ref": "#/components/parameters/idempotencyKeyPath"
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/list_capabilitiesResponse"
                }
              }
            },
            "description": ""
          },
          "404": {
            "$ref": "#/components/responses/InvocationNotFoundError"
          },
          "470": {
            "$ref": "#/components/responses/InvocationNotReadyError"
          },
          "default": {
            "$ref": "#/components/responses/GenericError"
          }
        },
        "summary": "list_capabilities",
        "tags": [
          "Get Output"
        ]
      }
    }
  },
  "servers": [
    {
      "description": "Restate Ingress URL",
      "url": "http://127.0.0.1:8080/"
    }
  ],
  "tags": [
    {
      "description": "Request response to handler",
      "externalDocs": {
        "url": "https://docs.restate.dev/invoke/http"
      },
      "name": "Request Response"
    },
    {
      "description": "Send to handler",
      "externalDocs": {
        "url": "https://docs.restate.dev/invoke/http"
      },
      "name": "Send"
    },
    {
      "description": "Attach to invocation",
      "externalDocs": {
        "url": "https://docs.restate.dev/invoke/http"
      },
      "name": "Attach"
    },
    {
      "description": "Get invocation output",
      "externalDocs": {
        "url": "https://docs.restate.dev/invoke/http"
      },
      "name": "Get Output"
    }
  ]
}
